# Pre-commit: format check only. Heavy checks (typecheck, lint) run in CI.
bun run format:check

# Check UI formatting if UI changes detected
if git diff --cached --name-only | grep -q "^ui/"; then
  echo "[i] UI changes detected, checking UI format..."
  cd ui && bun run format:check
fi
