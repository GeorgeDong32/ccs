# Simplified pre-commit: only enforce conventional commit message format.
# Full validation available via: bun run validate
echo "[i] Pre-commit hook: commit message format only (use 'bun run validate' for full checks)"
